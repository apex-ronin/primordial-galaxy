# PRIMORDIAL GALAXY — Roadmap

**Owner:** Jay Nelson | **Updated:** 2026-09-07 | **Session:** v19 (continued into 09-07, closes here)

> **Roadmap drift note (v19):** this file was last updated at v18 (2026-08-10),
> but git history shows 8 more commits landed after that before v19 started
> (`590c407` Heartland Phase 1 schema, `01a0a33` entity verifier, `efafd7f`
> state-portal cascade, `4da8e50` South region + compliance scan UI, `8661a9d`
> Ollama generation rewire — all 08-10 to 08-24) that were never logged here.
> v19 did not audit/backfill those commits individually (out of scope for a
> bootstrap session) — whoever picks this up next should `git log` that range
> before trusting this file's pre-v19 "front and center" section blindly.

> Global cross-project ledger = `G:\Workspaces\STATE.md` (the old OneDrive
> APEX_RONIN_FUNCTIONAL_ROADMAP.md did not survive OneDrive retirement; STATE.md
> is now canonical for cross-project state).

> **🔴 NEW SESSION START HERE (09-07, end of v19): two inbox docs from the Taichi-side thread need action.**
> `/home/apexronin/inbox/BOOTSTRAP.md` and `/home/apexronin/inbox/HANDOFF.md` just arrived —
> written by a *separate* Claude Code thread that remotely bootstrapped this exact box
> (10.239.1.148, apexronin, primordial-galaxy) from Taichi, 2026-08-31 to 2026-09-05,
> entirely disconnected from this session's knowledge until now. Real findings in them:
> 1. **🔴 URGENT, confirmed by Jay NOT yet done: 12 `.env` files with live credentials
>    were carried over in the migration and never rotated.** Full file list is in
>    BOOTSTRAP.md's 2026-09-05 session log entry (includes `repos/primordial-galaxy/.env`,
>    `repos/ronin/.env`, `repos/jarvis-stack/.env`, and 9 more under old Workspaces paths).
>    This is higher priority than anything else on this list — live creds, unrotated, 2+
>    days and counting as of this writing.
> 2. **BOOTSTRAP.md's "known-good facts" table still has the stale FAR 52.204-21 claim**
>    ("renumbered to 52.240-93") that this session verified wrong and fixed in the private
>    `legal-corpus` repo on 09-07 — don't let anyone (human or Claude) trust that table
>    as-is; it needs the same dual-numbering correction applied here too if that file is
>    kept.
> 3. **Real, still-useful hardware/ops facts in these docs, not yet folded in anywhere**:
>    Ed25519 SSH keys do not work against FIPS-hardened sshd (use RSA/ECDSA) — working key
>    is ECDSA, private half on the Taichi at `C:\Users\jnel9\.ssh\id_ecdsa_pg`; separate
>    2GB `/boot` partition confirmed (`nvme0n1p2`) so TPM-sealed LUKS can be added later
>    without a reinstall; two independently-verified corpus backup copies exist (Taichi
>    `M:\backups\G_drive_full_backup_2026-08-30` and external Samsung SSD
>    `X:\apexronin_corpus_2026-08-30`, both 222,730 files / 33.77GB); Taichi itself has
>    chronic liquid-damage instability (~6 crashes/24h) — never run long jobs on it.
> 4. **The bigger, systemic issue this surfaced (Jay, 09-07): "STATE.md" was supposed to
>    be the one global cross-repo source of truth, with each repo keeping its own roadmap
>    underneath it — this file (`primordial_galaxy_roadmap.md`) is exactly that per-repo
>    piece, working as intended.** The global piece broke: `STATE.md` lived at
>    `G:\Workspaces\STATE.md` — a Taichi-only drive letter — so it silently stopped being
>    reachable the moment work moved to a machine that can't mount `G:\`, and BOOTSTRAP.md/
>    HANDOFF.md got invented as an ad hoc replacement instead of migrating it. This is at
>    least the third time a "one true doc" has died this way (OneDrive doc → OneDrive
>    retirement; STATE.md → drive-letter tied to a retired machine). **Proposed fix, not
>    yet executed, Jay is weighing it**: a small dedicated git repo (e.g.
>    `apex-ronin-state`) holding the real cross-repo STATE.md, reachable identically via
>    git from any machine — survives the next machine change by construction instead of by
>    discipline. **One lead worth checking before assuming STATE.md is gone**: Jay noted
>    it may actually still exist on the corpus backup — the Samsung SSD is physically
>    mounted on *this* box already (see HANDOFF.md's corpus-load section, `/dev/sda2`) —
>    check there before concluding it needs to be reconstructed from scratch.
> 5. Minor/deferred: `gh` CLI is installed on this box at `/home/apexronin/.local/bin/gh`
>    but isn't on `PATH` for non-interactive tool calls — add `~/.local/bin` to PATH (or
>    call the full path) before assuming `gh` is unavailable here.
>
> **Also from the tail end of this session — governance state to know before touching
> anything cross-repo:** a parallel session ("Repository audit and recommendations",
> `local_fa5ce188`) was mid-way through fixing portfolio-wide exposure issues (see
> [[project-apex-ronin-portfolio-audit-2026-09-07]] in memory) — Jay is going there next
> to close it out, so check that session's status before assuming any of its open items
> (git-history purge, forbidden-paths review, scanner_daily.yml fix) are still open.

> **✅ RESOLVED 2026-09-09 — Open Decision #7 (public/private split model): Option A.**
> `main` = full private history (every commit, including Heartland/commercial-layer
> work) — pushed to `jsnnlsn` only, never to `origin`. A separate, persistent
> `public` branch receives only hand-vetted, genuinely-real, unaltered commits
> (reviewed cherry-pick, not squash) — pushed to `origin` only. Jay's reasoning:
> the public repo's committed history should be genuine and unaltered — it feeds
> a planned paper about the actual human-collaborative build process, mistakes
> included — so squashing/resetting history to fake a clean launch was explicitly
> rejected. This does NOT require hiding failed/incomplete early work from the
> public branch, only keeping private-commercial-IP and secrets off it. The
> `public` branch went live 2026-09-10 (`origin/public` = `041d431` initially,
> then a real curated content update to `b756e84` same day) — first real proof
> this model works mechanically, not just on paper.
>
> **Still `git push origin main` never happens** — that was always wrong under
> either split-model option discussed (this is unchanged, not newly decided).
> `main` currently contains the full Heartland entity verifier
> (`entity_procurement.py`) and `scripts/set_mirror_secrets.ps1`; both are
> deliberately excluded from `public`. See WHAT NOT TO DO.
>
> **Historical note, superseded above:** this section previously read "Jay's
> 09-06 call: don't decide the long-term split model yet, just do not push main
> to origin until a model is chosen" — that was accurate as of 09-06 but was
> never updated here after the decision landed on 09-09, leaving this file
> claiming UNDECIDED for a day after STATE.md already had the resolution.
> Exactly the "stale roadmap" failure mode this file's own footer warns about —
> flagged by Jay directly (2026-09-10), fixed here.

---

## STATUS

Core pipeline complete + **Observatory (live observability) built and proven running unattended.** Whitepaper published (v2.1, 2026-06-04). GCP fully torn down (2026-06-06) — clean slate, local-primary architecture. Legal corpus on disk (104 clauses, 8 files). Daily scanner clean 06-14→06-16 (Venice-served).

**Public flip: DONE.** `apex-ronin/primordial-galaxy` is live and public, confirmed intact 2026-08-08 (README, license, docs, 31 commits — verified via direct fetch, nothing missing).

**Cloud-failover scanner: DONE, not a blocker.** v17 (above, written by a companion chat session without STATE.md/HANDOFF access) re-flagged the 3 mirror secrets and `workflow_dispatch` test as pending. **Both were actually completed 2026-08-05** — verified fresh this session (2026-08-10): `gh secret list -R jsnnlsn-prog/primordial-galaxy` shows all 3 secrets set 2026-08-05; `gh run list` shows the scheduled workflow green every day 08-05→08-10 except one transient GitHub-hosted-runner infra failure on 08-06 ("not acquired by Runner") that self-healed the next day — not a config problem. **Do not re-litigate this.**

**eCFR fix + a real embed-pipeline bug it surfaced (09-07, uncommitted).** Jay's "keep every rule current" directive: added `observatory.ingest.fetch_ecfr_title48()` — pulls FAR/DFARS from govinfo's bulk eCFR Title 48 XML (continuously current, e.g. this session's pull showed a Mar-2026 amendment date) instead of `fetch_title48()`'s annual CFR snapshot (up to a year stale). Bonus: eCFR ships full text in one download, so far/dfars no longer need fulltext.py's separate fill pass. Also ingested **DFARS Part 252 for the first time ever** (405 sections) — `corpus_docs` never had a `dfars` source at all before this, despite the code nominally supporting it. **Found while wiring this up**: `db.py`'s `upsert_corpus_doc` embedded-guard (from the 09-06 EO fix) was too blunt — "once embedded=1, stay 1 forever" also blocked a legitimate re-embed when text genuinely changes to different content. Fixed: embedded now resets to 0 only when incoming text is non-empty AND differs from what's stored; unit-tested all 3 cases (empty-doesn't-clobber, real-change-resets, unchanged-stays-put).

**Bigger find: `embed_corpus.py` never called `load_dotenv()`.** Every embed run this session (including the "successful" 1887-vector one hours earlier) silently wrote into a throwaway directory (`./G:\AI-Models\indexes/` — a legal-if-odd literal Linux dirname, not a path, since `RONIN_INDEX_DIR` was never actually read from `.env`) instead of the real configured index dir (`/home/apexronin/pg/models/indexes/`, per `.env`) that `antibody_agent.py` (which does call `load_dotenv()`) actually reads at query time. **Net effect: today's EO full-text fix and the new eCFR content were never actually retrievable until this was caught and fixed** — every "grounded: true" citation seen earlier today was grounding against the stale 2026-08-10 index, not today's work. Same latent bug (env read with no local `load_dotenv()`) also found and fixed defensively in the brand-new `observatory/archive.py` (not yet load-bearing — main.py happens to load .env first — but fragile) and `entity_procurement.py` (not currently load-bearing either — `RONIN_PRINCIPALITIES_JSONL` isn't set in `.env` — but same fragile pattern). Re-ran the embed for real: 2,307 vectors now at the correct path; live-verified DFARS content is actually retrievable (`_retrieve_records()` on a cyber-safeguarding query returns `DFARS 252.204-7012`/`7008`/`7009` alongside FAR hits). The stray wrong-path directory was moved (not deleted) to `data/_archive_pre_clean_2026-09-07/stray_wrong_path_index_dir/`.

**Permanent opportunity archive + delta pipeline (09-07, uncommitted) — Jay's directive, built same-day.** New `opportunity_documents` table (`observatory/db.py`) + `observatory/archive.py`: every opportunity's full fetched document text is now permanently archived to `data/opportunity_archive/<link-hash>.txt` (gitignored, covered by the existing bare `data/` line) regardless of score, content-hash-deduped so re-fetching identical content is a no-op, `retention_until` tracked (default 730 days, `ARCHIVE_RETENTION_DAYS` env-overridable) — no auto-prune job yet, metadata only. **Pipeline is now delta-aware**: `main.py`'s Phase 2 checks `observatory.db.get_opportunity_by_link()` (the existing `opportunities` table already accumulates one row per link across every run — no new table needed for this half) before paying for a fetch + LLM score; a previously-seen link reuses its stored `raw_json` verbatim (tagged `delta_status: "reused"`, `analysis_method` left untouched so it still reflects how it was *actually* originally scored — a separate field, not an appended tag, specifically to avoid the tag stacking on itself across repeated future reuses of the same opportunity) instead of a fresh fetch+LLM call. Unit-tested in isolation (new-link/known-link lookup, archive write/dedup/change-detection, disk content verified) — not yet exercised by a real end-to-end run since the in-flight run at time of writing started before this change landed.

**🔴 Jay's explicit gate (09-07): no outreach, no AF Tech Connect submission, until the pipeline runs consistently error-free and produces real (not placeholder/false-positive-triggered) clauses and red-teaming.** Confirmed architecture finding behind this: `opportunities.json` and `threat_assessment.json` reset every run (by design — last-run snapshot), but `data/procurement_shield_pending.json` is **append-only with no reset and no promotion/rejection workflow** (still just "the landing zone," see SPEC GAPS) — it silently accumulates good and bad clauses together forever unless a human manually intervenes. Concretely hit this 09-07: the queue held one stale pre-fix test artifact plus one clause generated against the confirmed false-positive HIGH-fit from the first full run (09-06) — both wiped to `[]` (backed up to `data/_archive_pre_clean_2026-09-07/`) rather than cherry-picked, since neither represented trustworthy output. **This will recur** until either (a) a real promotion/rejection workflow exists, or (b) someone manually audits the queue after every run — don't assume a populated `procurement_shield_pending.json` is trustworthy just because it's non-empty.

**Real front-and-center items for next session** (from cross-referencing `G:\Workspaces\STATE.md` tail + `HANDOFF_2026-08-05.md`, none of which made it into v17):
1. ~~Embed the 1,060 `far_rfo` docs into FAISS~~ — ✅ **DONE 08-10.** The LM Studio investigation above turned out to be moot: Jay deleted LM Studio from the box entirely (deliberate cleanup, not a full cloud pivot — local-primary architecture stands). Fix: `observatory/embed_corpus.py` repointed at Ollama's `nomic-embed-text` (already installed, same model, headless — no more Electron-GUI crash exposure), CPU-pinned (`num_gpu: 0`) since `OLLAMA_VULKAN=1` is set on this box and the RX580 is the display GPU — same TDR risk already root-caused for LM Studio, and nomic-embed-text is 137M params so CPU costs nothing. Full `corpus_docs` index rebuilt live: 1,788 docs, all 1,060 far_rfo now `embedded=1`. Commits `4e8101b` (fix) + `78d5616` (CLAUDE.md correction). **New gap this creates:** `execution/llm_client.py`'s local *generation* tier is still hardcoded to LM Studio's port 1234 and is now permanently dark (not just intermittently down) — cascade falls to Venice, which already works fine. **This is not a fast-follow to casually close** — see the Local generation tier row in System State below and `[[hardware-crash-under-local-compute]]`: sustained local generation crashed the box three times on 2026-07-21, CPU-only included, so rewiring to Ollama's `gemma4:12b` needs the suspected PSU/power issue addressed first, not just a code change.
2. **American-models hygiene now extends past LM Studio.** Found `qwen3.6` (Chinese) and `minimax-m3:cloud` (Chinese, Ollama-cloud-routed) sitting in Ollama's own library — unrelated to the June LM-Studio-specific scrub, same problem in a different tool. Deleted both, plus the entire June `_quarantined_models` folder (83GB — was moved-not-deleted; Jay's 08-10 directive going forward is delete-on-sight, not quarantine-and-flag). **Standing directive for future sessions: check every local model runner (LM Studio, Ollama, anything else added later) for non-American models, and delete on sight — don't just flag for later review.**
2. **🔴 Regulatory correction sweep.** STATE 2026-08-05: FAR 52.204-21 was **not** actually renumbered in the codified FAR — it's still live at HEAD on acquisition.gov. 52.240-93 is a separate RFO Part 40 class-deviation number (DHS 25-23 etc.); both are citable, dual-numbering era. The old flat "renumbered to 52.240-93" claim from the June audit propagated into repo docs, the curated legal corpus, and the whitepaper. Sweep to dual-numbering language — gated on Jay's review (compliance-adjacent).
3. `LMStudioServerAtBoot` still not registered — recurring gap across many sessions, needs Jay running `register_boot_task.ps1` from an elevated terminal (Claude is blocked from SYSTEM-persistence changes by design).
4. July catch-up backfill — SAM `postedFrom`/`postedTo` sweep of the ~06-30→08-04 window — never run.
5. Arc #6 "Heartland" Phase 1 spec (`ARC6_HEARTLAND_PLAN_2026-08-05.md`) — entity_procurement table + Midwest verifier job — not started.
6. **Air Force Tech Connect submission status unconfirmed.** Package is ready (`Outreach/AF_TECH_CONNECT_SUBMISSION_2026-08.md`), HANDOFF_2026-08-05.md lists it as a Jay action item ("Submit AF Tech Connect... Screenshot confirmation → STATE line"), but no STATE.md line confirms it was sent. **Ask Jay directly rather than assuming either way.**

Scott Nelson engagement permanently closed — building on merit, own timeline.

**Note for whoever picks up next (human or Code):** this file drifted from ground truth between v16 and v17 because a companion chat session updated it without reading `G:\Workspaces\STATE.md`'s tail or `HANDOFF_2026-08-05.md`. **Always check STATE.md's tail and the most recent HANDOFF_*.md before trusting this file's "front and center" section** — this file is a snapshot, STATE.md is append-only ground truth.

---

## WHAT IS COMPLETE — DO NOT RELITIGATE

| Item | Commit | Notes |
|---|---|---|
| B-1: random seed entropy | 2a970a2 | `random.seed(int(time.time()))` in red_team_simulation.py |
| B-2: real ROI values | 2a970a2 | `parse_opportunity_value()` reads actual contract values |
| B-3: antibody collection loop | 2a970a2 | procurement_shield.json populated each run |
| B-4: budget guard before truncation | 2a970a2 | Guard fires on original input, not post-truncation |
| B-5: false AaaS proxy reference | 2a970a2 | csda_honey_pot.md updated |
| B-7: SSH -i flag | 2a970a2 | satellite_dashboard_ops.md corrected |
| B-8: grant_hunter PII exception | 2a970a2 | git_publication_safety.md updated |
| B-9: pureswarm refs in backlog | 2a970a2 | backlog_and_reminders.md updated |
| Vertex AI migration | 183aec2 | gemini_analyst.py, red_team_simulation.py, antibody_agent.py — zero google.generativeai imports |
| antibody_agent.py built | 183aec2 | Corpus retrieval, drafting (was Vertex; now direct Anthropic API cascade), specificity scoring (≥75 gate), economic calibration |
| Legal corpus seeded | a1f740a | Originally 32 clauses; expanded to 104 (25 FAR + 8 CA + CMMC/cyber/data-rights/GSAR domains) |
| Grant Hunter (Item 18) | 183aec2 | 4-source pipeline: grants.gov + sbir.gov + browser portals + seeds |
| GTG-1002 whitepaper preface | 183aec2 | directives/WHITEPAPER_PREFACE.md |
| OMB policy reference | 726440d | directives/OMB_AI_POLICY_REFERENCE.md — M-25-21/M-25-22 |
| SAM.gov API key rotated + live | — | Verified live 2026-04-14 |
| peer_review_package/ gitignored | 7741d6b | Contained exposed SAM API key — never committed |
| Roadmap system established | 7741d6b | This file is session SoT. directives/ files archived. |
| Legal corpus expanded to 104 | 1d7816b | 6 new domain files: cmmc, cyber, data-rights, small-biz, IT-cloud, GSAR |
| Corpus ingested to Vertex | 1d7816b | 104 docs ingested (HISTORICAL — Vertex datastore deleted in 2026-06-06 teardown; corpus now served by local nomic-embed FAISS) |
| Antibody drafter → Claude Sonnet | 1d7816b | Swapped from Gemini Flash to claude-sonnet-4-6 (Vertex stage later removed; now direct Anthropic API via llm_client cascade) |
| M-26-04 disclosure (item 6) | 1d7816b | Wedge injected to antibody prompt; superseded 2026-06-03 by stamp function |
| M-26-04 disclosure_template.py | — | `execution/compliance/disclosure_template.py` — canonical 4-element stamp_disclosure(). Replaces TTSI_SPEC_v2 runtime gate. |
| TTSI_SPEC_v2 reviewed + killed | — | Runtime gate was wrong layer for current scale. Disclosure is a human-review + stamp obligation. See Settled Decisions. |
| **Observatory — live dashboard + run recorder** | v16 (06-16) | `observatory/` pkg: SQLite spine (`data/primordial.db`: runs/opportunities/corpus_docs), FastAPI live dashboard (`run_dashboard.ps1`, :8787), non-fatal recorder hook in main.py. **Verified auto-recording unattended** (runs #2,#3). Surfaces which LLM tier served — caught local tier down 3 days. |
| **govinfo.gov research + ingest** | v16 (06-16) | `docs/GOVINFO_RESEARCH_2026-06-14.md` + `observatory/ingest.py`. EOs via Federal Register API (current to EO 14411), GAO via govinfo collections (historical archive). 74 docs seeded into corpus_docs. Sets up arc #4 (antibody ORACLE). |
| **Pre-public history audit (checklist)** | v16 (06-16) | `G:\Workspaces\GITHUB_PRE_PUBLIC_CHECKLIST.md` (reusable). Ran live: secrets CLEAN (no key ever in history); **PII BLOCKER** — [REDACTED] NSF contact in 7 historical commits + 4 tracked docs. Scrub staged (Method A), not executed. |
| Cloud-failover scanner secrets + workflow_dispatch test | — (08-05, verified 08-10) | All 3 mirror secrets set on `jsnnlsn-prog/primordial-galaxy` 2026-08-05; `workflow_dispatch` tested same day; schedule green 08-05→08-10 (one transient runner-infra blip 08-06, self-healed). |
| RFO-current FAR ingest module | dab4249 (08-10) | `observatory/ingest.py` `fetch_far_rfo()` — acquisition.gov FARHTML.zip, RFO-current text. Run live 08-05: 1,060 sections (parts 3/9/15/19/52) ingested. **Embedded 08-10** (`4e8101b`, via Ollama) — antibody retrieval now covers the RFO-current text. |
| Mirror-secrets sync script | 310bf27 (08-10) | `scripts/set_mirror_secrets.ps1` — pushes `.env` vars to `jsnnlsn-prog/primordial-galaxy` Actions secrets with name-mapping. Used to unblock the item above on 08-05. |
| **primordial-galaxy node bootstrap (v19)** | uncommitted -- git sync deferred, see below | New Linux box (Ubuntu 22.04 FIPS, i5-10400, Intel UHD 630 integrated only -- **no discrete GPU**, physically distinct from the old Taichi X370/RX580 rig). Bootstrapped from scratch: venv + `requirements.txt`, Ollama installed **without root** (extracted release tarball to `pg/ollama-bin/`, `OLLAMA_MODELS` pointed at the pre-migrated blob store at `pg/models/ollama` -- `gemma4:12b`/`nomic-embed-text` loaded with zero re-download). Fixed 3 hardcoded `G:\` Windows paths (`observatory/embed_corpus.py`, `observatory/entity_procurement.py`) to use the existing `RONIN_INDEX_DIR` env-override pattern / repo-relative sibling-checkout paths instead. |
| **Semantic retrieval bug fix (v19)** | uncommitted | Real bug, predates this session: the 08-10/08-24 LM-Studio-to-Ollama rewire changed `LOCAL_LLM_BASE_URL` to a bare address for `llm_client.py`'s native `/api/chat` calls, but `antibody_agent.py`/`health_check.py` still built an old-style `/v1/embeddings` URL from that same var -> 404 every time, silently degrading **every** antibody clause draft to keyword-only retrieval since the rewire. Also found the `legal_corpus`/`principalities` FAISS manifest entries still named the embedder `text-embedding-nomic-embed-text-v1.5` (LM Studio's name) instead of Ollama's tag `nomic-embed-text` -- same model, wrong name, same 404. Fixed both; verified live (a real query now returns grounded DFARS/OMB clauses). `corpus_docs`'s embedder name was already correct (fixed in the original 08-10 rewire) -- only `legal_corpus`/`principalities` had drifted. |
| **Local model right-sized for this box (v19)** | uncommitted (`.env`, `llm_client.py`) | `gemma4:12b` (dense, chosen on the old box assuming GPU offload) benchmarked unusably slow on this CPU-only i5-10400 -- two attempts at a ~150-word "fast"-mode reply never finished inside 230s. Swapped to Gemma's elastic variants along the code's existing fast/precise split: `LOCAL_MODEL_FAST=gemma4:e2b` (~3.2 words/sec, clean bench) / `LOCAL_MODEL_PRECISE=gemma4:e4b` (~1.5 words/sec). Also removed the 65%-CPU/`num_gpu=31` throttle in `llm_client.py` (Jay's response to RX580 TDR crashes on the old box) -- that failure mode doesn't exist on this box's integrated-only graphics, so `LOCAL_CPU_THREAD_CAP`/`LOCAL_NUM_GPU` now default to `None` (omitted from the Ollama request, letting it auto-tune) instead of a hardcoded cap. `.env`'s `LLM_MODE` flipped from a hard `venice` pin back to `auto` -- confirmed live, local tier now actually serves (`get_last_provider()` -> `local (gemma4:e2b)`). |
| **Playwright working on Linux (v19)** | uncommitted (`.env` unaffected) | `sudo playwright install-deps chromium` initially failed silently -- `sudo` doesn't inherit venv `PATH`, needed the full venv path (`sudo /home/.../.venv/bin/playwright install-deps chromium`). Once installed, verified live: CSDA scraper found 7 real items (previously always failed with a missing-deps error), grants-browser portals now navigate (0 opportunities found this run, but that's real site content, not a crash). |
| **Grounded antibody pipeline wired into main.py (v19)** | uncommitted | `antibody_agent.py`/`red_team_simulation.py` were fully built (Item 24, semantic FAISS retrieval, grounding gate, specificity rubric >=75) but **orphaned from the live pipeline entirely** -- zero callers in `main.py`, `data/procurement_shield.json` untouched since 2026-06-11 despite 3 months of daily unattended runs. Root cause of the orphaning: `red_team_simulation.py`'s old CLI `main()` blocked on `input()` ("Finding AS-02: Mandatory Human Gate") -- incompatible with Task Scheduler/cron's no-console execution. Refactored: extracted `run_batch()`/`save_threats()` (reusable, no blocking prompt) into `red_team_simulation.py`; `main.py` now has a Phase 3.5 that calls both on `high_value` opportunities only (bounding nightly runtime -- each item is 2 precise-mode local LLM calls). Gate replaced with an async pending-review file, `data/procurement_shield_pending.json` (Jay's choice over dropping the gate entirely) -- nothing auto-promotes into the canonical `procurement_shield.json`. Verified end-to-end against a real HIGH-fit opportunity: `validation_status: VALIDATED`, `grounded: true`, real citation (`FAR 3.405`), specificity score 85. Took ~4.6 min for that one item. |
| **Corpus ingest refreshed (v19)** | uncommitted (DB + FAISS index, not code) | EO ingest was stuck at EO 14411 (2026-06-03) -- 3 months stale. Pulled through EO 14423, re-embedded `corpus_docs` (still 1788 embedded vectors -- **see gap below**, the 59 new EO rows have empty text and were correctly skipped, not a bug). FAR/DFARS/RFO/GAO indexes unaffected, still fully current and embedded. |
| **Full live pipeline run, end to end (v19)** | run #30 in `data/primordial.db` | CSDA 7 + SAM.gov 13 + Grant Hunter 43 = 63 opportunities, all local generation (`e2b` fast-mode scoring), 87 min wall time, 5/5 health check green, 1 HIGH-fit result with real (if less-rigorous, pre-antibody-fix) red-team/immune_system output. Confirms the whole local-primary stack works unattended on this box. |

~~| **Full pipeline run, end to end, post-fixes (v19 continued, 09-06)** | run #31 in `data/primordial.db` | 65 opportunities (CSDA 7 + SAM 15 + Grant Hunter 43), 93 min wall time, **0 cloud fallback** (67/67 LLM calls served `local (gemma4:e2b/e4b)`, 0 venice/anthropic — Jay's fail-flag #1, clean), **0 emergency-failsafe antibodies** (fail-flag #2, clean) — but caught fail-flag #3 live: 7/65 scoring calls silently degraded to the keyword-placeholder fallback, including the one HIGH-fit opportunity that drove the entire Phase 3.5 antibody run. Root-caused (JSON parse failures, see SPEC GAPS) and fixed same-session; fix re-verified against all 7 original failures (7/7 clean after the fix). This is the first time the full designed pipeline — scrape → score → red-team → grounded antibody, at real batch scale, not a 1-item smoke test — has actually run end to end on this box. |

**Known gap surfaced, not fixed (v19):** new EO ingests have empty `text`~~ ✅ **RESOLVED (09-06, uncommitted).** Root cause confirmed: Federal Register's `abstract` field is empty for virtually all EOs (EOs don't carry a summary — the text IS the document). Fix: `ingest.py`'s `fetch_executive_orders()` now also captures `raw_text_url` into `meta_json`; `fulltext.py`'s `_url_for()` routes `source="eo"` there (same `_strip_html` pipeline already used for FAR/GAO). **Second bug found in the process**: the raw Federal Register .txt files carry literal NUL bytes (page-break artifacts) — Python's sqlite3 driver silently truncates a stored string at the first NUL with no exception, so an initial test fill only captured 288 of 17,545 real characters. Fixed in `_strip_html` (strip NULs before storing). **Third, adjacent bug found and fixed**: `db.py`'s `upsert_corpus_doc` unconditionally overwrote `text`/`embedded` on conflict — once scheduling (below) starts re-running ingest on a cron, any EO text `fulltext.fill()` backfilled would've been silently wiped back to empty by the next ingest sweep. Guarded: a re-ingest can no longer replace non-empty `text` or `embedded=1` with empty/0. Backfilled + re-embedded: all 99 EO rows (some newly pulled further back in history by the re-ingest) now carry full real text (avg ~8.7k chars, 0 remaining NULs).

**First real unattended cron run — the 07:00 daily scheduling actually works (run #33, 09-07).** `crontab`-fired, not manually triggered: 07:00:01→07:07:43, 67 opportunities, exit success, tier `local`. Fast (7.5 min vs. the ~90 min pre-delta baseline) because the archive/delta system was already live by the time it fired.

**Delta pipeline validated for real (run #33 cron + run #34 manual, 09-07), and a real gap it surfaced, fixed same-session.** Phase 2 delta-check confirmed working as designed (run #34: 2 new scored, 50 reused, ~5 min). **But caught live, twice**: Phase 3.5 (red-team + antibody drafting) never checked `delta_status` at all — the same HIGH-fit opportunity from run #32 ("Development of Candidate Medical Countermeasures..."), correctly *reused* in Phase 2 on both run #33 and run #34, still got a full fresh red-team + antibody draft each time in Phase 3.5, appending two near-duplicate clauses to `procurement_shield_pending.json` before this was caught. Concrete proof this matters, not just theory: run #34's fresh draft was actually **worse** than the original (`POTENTIAL_BLEED` vs. the original's `ENFORCED`) — LLM non-determinism means re-running this step on unchanged input can silently *regress* quality, not just waste the 2 precise-mode calls it costs each time. Fixed: Phase 3.5 now filters to `delta_status == "new"` only. Removed both duplicate entries from the pending queue — back down to the 1 legitimate entry from run #32.

**First fully clean full-batch run, all three of Jay's fail flags passing (run #32, 09-07).** 66 opportunities, 68/68 LLM calls local (0 cloud fallback), 0 emergency-failsafe antibodies, 0 keyword-placeholder fallbacks (100% real `LLM Dual-Track [local]` scoring — the `json_mode` fix from run #31 held across a full batch). The 1 HIGH-fit ("Development of Candidate Medical Countermeasures...", score 75, real Outsourcing-Fraud red-team) produced a real, grounded, specific antibody clause: named actor (COR/C3PAO), real cadence + consequence, `FAR 3.405`, `ENFORCED` (`$125K` compliance-evasion cost, `0.4` post-clause ROI). This is the baseline "consistently reliable" run Jay's outreach gate is waiting for — one clean run isn't "consistent" yet, but it's the first proof the fixed pipeline can produce one end to end.

**Regulatory correction sweep — RESOLVED (09-06), not just flagged.** Verified live against acquisition.gov (not just trusted from the 08-05 note): FAR 52.204-21 returns 200 with the correct title (still the live codified clause); 52.240-93 404s as a FAR clause page. Same pattern confirmed for the DFARS pair via the actual subpart listing page (252.204-7020 present, 252.240-7997 absent). The stale flat "Renumbered to X" claim only existed in the **private** `legal-corpus` repo's `cmmc_cyber_clauses.json` (confirmed NOT public — `apex-ronin/legal-corpus` 404s unauthenticated; also confirmed it never propagated into primordial-galaxy's own public sample corpus or the public whitepaper). Corrected both entries (FAR 52.204-21, DFARS 252.204-7020) to accurate dual-numbering language with dated verification and the exact source URLs, per Jay's explicit approval of that approach — done in the `legal-corpus` repo, not this one.

---

## OPEN DECISIONS

| # | Question | Blocks? |
|---|---|---|
| 1 | LLC filing — deferred to post-revenue. Sole prop until first paying client. | Does not block pipeline or outreach |
| 2 | ~~Whitepaper publish~~ | ✅ CLOSED — v2.1 live at apex-ronin/Rise-Of-The-Prompt-Kiddie (2026-06-04) |
| 3 | **Public-flip: GO in principle (Jay), but GATED on a clean scrub.** Current-files audit passed, but HISTORY is not clean ([REDACTED] PII). Decision: keep history → **Method A surgical scrub** (filter-repo, redact PII strings + drop 4 internal docs), NOT re-init. | Blocks flip until scrub done + checklist re-run zero-hits |
| 4 | ~~Legal corpus in the public repo — open / sample / closed?~~ | ✅ **RESOLVED, mislabeled UNDECIDED through v19** — verified live 09-06: public `main` on GitHub ships a 4-clause **sample** corpus (`data/legal_corpus/far_clauses.json`), not the full 104. This was decided and shipped via the `ops` branch (2026-08-05, `0654255` "feat(corpus): add public sample corpus (open-core)") but never written back to this file. Full 104-clause set stays private-layer. |
| 5 | ~~License — permissive vs noncommercial~~ | ✅ **RESOLVED, mislabeled UNDECIDED through v19** — **PolyForm Shield 1.0** is live in `LICENSE` on the public repo (via `ops` branch, `f3e9c86` "chore: public-release prep — PolyForm license"), never written back to this file. |
| 7 | ~~Public/private split model going forward~~ | ✅ **RESOLVED 2026-09-09 — Option A.** `main` = full private history, `jsnnlsn` only. Persistent `public` branch = hand-vetted, unaltered commits only, `origin` only. See the critical note at the top of this file for Jay's full reasoning and the 2026-09-10 first-real-push status. | Does not block `git push jsnnlsn` at all; `git push origin main` (the full branch) is still never correct under this model — only `git push origin public` (or PR) is |
| 6 | **American-models-only** — VERIFY the regulatory basis (is there a *binding* rule, or just EO 14179 pro-dominance + congressional urging on FAR/PRC?). Then swap qwen daily-driver. | Strategically sound for govtech regardless; verify before stating as compliance |

---

## SPEC GAPS (backlog — not bugs)

| Gap | File | Priority |
|---|---|---|
| ~~Corpus retrieval is keyword-equality match~~ | `execution/antibody_agent.py` | ✅ RESOLVED — semantic nomic-embed FAISS retrieval + grounding gate (branch `pipeline-hardening-2026-06-11` @ 813726d) |
| ~~Old M-26-04 wedge stub still in antibody_agent.py~~ | `execution/antibody_agent.py` | Verify on the hardening branch; superseded by grounding gate + stamp function |
| ~~GEMINI_API_KEY declared in red_team_simulation.py~~ | `execution/red_team_simulation.py` | ✅ RESOLVED — module rewired to llm_client cascade; Vertex imports + GEMINI_API_KEY removed (2026-06-11 legacy audit) |
| ~~Drafter prompt does not request `actor`/`teeth` though the rubric scores them~~ | `execution/antibody_agent.py` | ✅ RESOLVED (09-06, uncommitted) — prompt now explicitly requires a named enforcement actor (CO/COR/C3PAO/DCAA/SPRS) + concrete consequence, plus per-vector mechanism guidance ported over from `red_team_simulation.py`'s (now-removed) discarded TASK 2. Live-verified: `actor` 0→15, specificity 85→100 on the same test case. |
| ~~`economic_calibration` structurally unreachable~~ | `execution/antibody_agent.py` | ✅ RESOLVED (09-06, uncommitted) — **found, not just fixed**: the old gate compared `specificity_score/10` (max 10) against `roi_multiplier` (~50x by construction for any contract >$50k, since `red_team_simulation.py`'s attacker cost is a flat 2% of payout) — different units, comparison could never pass, `ENFORCED` was unreachable on any real opportunity. Replaced with a real dollar-for-dollar model: drafter now also estimates `estimated_compliance_evasion_cost_usd` (cost to fake compliance with *this* clause); gate compares `payout / (original_attacker_cost + evasion_cost)` against 1.0. Live-verified: `post_clause_roi_multiplier: 0.11`, `ENFORCED`. Nothing downstream consumed the old field, so this was silent (would only have misled a human reviewing `procurement_shield_pending.json`), not a live bug. |
| Redundant discarded LLM draft in `red_team_simulation.py` | `execution/red_team_simulation.py` | ✅ RESOLVED (09-06, uncommitted) — `red_team_analysis()`'s TASK 2 drafted a full `immune_system_antibody` clause every call that `run_batch()` never read (it always uses `antibody_agent.generate()`'s separate draft instead) — one wasted LLM call per opportunity. Removed TASK 2; its vector-specific guidance moved into `antibody_agent.py`'s prompt (the one that's actually scored/saved), see above. |

| ~~JSON parse failures silently degrading scoring to the keyword-fallback placeholder~~ | `execution/llm_client.py`, `execution/gemini_analyst.py`, `execution/red_team_simulation.py`, `execution/antibody_agent.py` | ✅ RESOLVED (09-06, uncommitted) — **found via a real 65-item pipeline run, not theory**: 7/65 (11%) of scoring calls silently fell back to `keyword_score()` (Jay's explicit fail-flag #3, "boilerplate placeholder instead of a real score"), all from the identical `json.loads` error class ("Expecting ',' delimiter") — the small local model (`gemma4:e2b`) occasionally emits an unescaped quote inside a JSON string field. Root cause: `llm_client.py` relied on prompt instructions alone ("output strictly valid JSON") with no actual JSON enforcement. Fix: added a `json_mode` param threaded through `complete()` → Ollama's grammar-constrained `format: "json"` (structurally impossible to emit invalid JSON, not just less likely) and Venice's equivalent `response_format`; the three JSON-schema callers now opt in. **Live-verified**: re-ran the exact 7 failed items post-fix — 7/7 clean, zero fallbacks. **Consequence that made this worth fixing immediately, not just logging**: one of the 7 was "Staff Support for Regional Engagement..." — the single HIGH-fit (score 80) opportunity that triggered that run's entire Phase 3.5 antibody/red-team pipeline. Re-scored with real AI analysis (the fix applied): **45 (Medium)** — the HIGH-fit trigger was a false positive from the very bug it was hiding. The antibody clause generated against it (`data/procurement_shield_pending.json`) is still a valid drafting exercise but was prioritized on a fabricated signal. |
| `gemini_analyst.py` drafts a 3rd, ungrounded `immune_system_antibody` on *every* opportunity (fast-mode, no FAISS retrieval, no specificity/grounding gate) — stored as `opportunity['immune_system']`, distinct from the real grounded one `antibody_agent.py` produces for `high_value` opportunities only. Not currently mislabeled/exposed (not shown in the Observatory dashboard, `observatory/db.py:239` just stores it), so low-risk as-is, but worth deciding: keep as a cheap teaser signal for scoring, or drop the sub-object and save the output tokens since nothing reads it? | `execution/gemini_analyst.py` | Low — not misleading anyone today, just redundant compute |

---

## EXECUTION ORDER — NEXT SESSION

*(Public flip, American-models scrub, Arc #4 phases 1-4, and Scott Nelson are all CLOSED — see WHAT IS COMPLETE. Do not relitigate. Items 1-7 below are pre-v19, unaudited against the 08-10→08-24 commit gap noted at the top of this file — verify against `git log`/STATE.md before assuming they're still accurate.)*

**v19 front-and-center (this box, primordial-galaxy):**

0. **FIPS: do not upgrade the kernel or FIPS-related packages without verifying it holds FIPS 140 validation first.** `apt`/`apt-cache policy` show no update actually pending as of 2026-09-06 (`linux-image-fips`/`linux-fips` already at candidate `5.15.0.190.111`, no `/var/run/reboot-required`), but Jay saw something suggesting one was available in a separate terminal session. If a real kernel update shows up later, treat it as a stop-and-ask item, not a routine `apt upgrade` — this is a govtech-compliance-adjacent box and FIPS validation is tied to a specific certified kernel build, not just "newest is fine."
1. **Git sync is not configured on this box.** No SSH key, no credential helper, HTTPS fetch fails with no auth (`git@github.com: Permission denied (publickey)`). Jay chose a PAT over a deploy key or `gh auth login`, but deferred providing one this session ("skip git for now"). Until this is set up, all of v19's work (see WHAT IS COMPLETE above) exists only as an uncommitted working tree on this box — get a PAT from Jay, wire it into git's credential store, and get this session's changes committed + pushed to both `origin` (public) and `jsnnlsn` (private mirror) before anything else, so the work isn't sitting only on local disk.
2. ~~No scheduling set up yet on this box.~~ ✅ **DONE (09-06).** `run_scanner.sh` added (bash port of `run_scanner.ps1`: dated log in `logs/`, `SCAN_LOG_PATH` for the Observatory recorder, 30-day log trim, exits with the pipeline's real status). Cron entry installed: `0 7 * * * .../run_scanner.sh >> logs/cron.log 2>&1` (`crontab -l` was empty; this is now the only entry). Not yet verified by an actual unattended firing — first real test is tomorrow's 07:00 run.
3. **Review `data/procurement_shield_pending.json`.** v19 wired the grounded antibody pipeline in (see WHAT IS COMPLETE) with output landing in this new pending file instead of the canonical `data/procurement_shield.json`, by design (Jay's choice — async review over a blocking gate). Currently has exactly 1 entry (the Silvio O. Conte antibody clause from the manual test). Decide + build the actual promotion workflow (manual review + copy to `procurement_shield.json`? a script? a dashboard view?) — not designed yet, just the landing zone.
4. Consider a full live pipeline re-run at scale now that Playwright + local models + the antibody wiring all work together — v19's full run (63 items, 87 min) predates the antibody-pipeline wiring going in, and the antibody wiring's only been tested against 1 real HIGH-fit item in isolation. Worth seeing it run against a larger HIGH-fit batch together end to end.
5. Optional disk cleanup, not urgent: `gemma4:12b` (7.5GB, no longer used, superseded by e2b/e4b) and the stale Windows-binary `ms-playwright` copy under `pg/models/ms-playwright` (510MB, dead weight — this box installed its own Linux Chromium via `playwright install chromium` into the default location instead) are both sitting on disk unused.
6. ~~Real fix for the EO full-text gap~~ ✅ DONE (09-06) — see WHAT IS COMPLETE.

**Pre-v19 items (STATE.md/HANDOFF-sourced, last verified 08-10 — re-verify before acting):**

7. ~~Regulatory correction sweep~~ ✅ DONE (09-06) — see WHAT IS COMPLETE. Only the private `legal-corpus` repo needed it; nothing in this repo's docs/corpus/whitepaper carried the stale claim.
8. July catch-up backfill — SAM `postedFrom`/`postedTo` sweep of ~06-30→08-04.
9. Confirm AF Tech Connect submission status with Jay directly; if not sent, package is ready at `Outreach/AF_TECH_CONNECT_SUBMISSION_2026-08.md`.
10. Untracked scripts in `scripts/` awaiting a keep/delete call: `show_env_names.ps1` + `uncomment_venice.ps1` (one-off .env diagnostics, low future value), `verify_rfo_ingest.py` (docstring says "delete after review"), `probe_demolition.py` (unrelated one-off, proves long-tail thesis — may be worth keeping as documented evidence).

---

## SYSTEM STATE

| Component | Status | Notes |
|---|---|---|
| Hetzner CPX32 | DEAD | Shutdown verified 2026-04-06. |
| GCP — all 5 projects | DELETED ✅ | Torn down 2026-06-06. Clean slate. org jsn-nlsn-org retained. |
| Anthropic Pro subscription | LIVE ✅ | Restored 2026-06-02 |
| Inference layer | LOCAL — Claude via API | Vertex endpoints gone. Claude Sonnet via Anthropic API directly. |
| Legal corpus | ON DISK ✅ | 104 clauses, 8 source JSONs at `data/legal_corpus/`. Vertex datastore gone — source files are ground truth. |
| Antibody drafter | CLAUDE SONNET ✅ | claude-sonnet-4-6 via Anthropic API (Vertex endpoint deleted with project) |
| Vertex Discovery Engine | DEAD | Deleted with govtech-control. Bug 1 (Google Discovery 0 results) is now permanent until cloud rebuilt. |
| Grant Hunter | SCHEDULED ✅ | `GovTechHunterDaily` 07:00 daily. Clean runs 06-14→06-16. |
| Observatory (local) | LIVE ✅ | `observatory/` — SQLite + FastAPI dashboard (`run_dashboard.ps1`). Auto-records every run. Distinct from the old cloud HUD. |
| Local embeddings tier | LIVE ✅ (08-10, re-verified 09-06 on the new box) | Ollama `nomic-embed-text` serves retrieval embeddings. On the old box this was CPU-pinned to dodge an RX580/Vulkan TDR risk; **on primordial-galaxy that risk doesn't exist at all** (Intel UHD 630 integrated only, no discrete GPU). v19 found + fixed a real bug: `antibody_agent.py`/`health_check.py` were hitting a stale `/v1/embeddings` URL (404) and the `legal_corpus`/`principalities` manifest entries still named the embedder with LM Studio's old name — both fixed, semantic retrieval confirmed working end-to-end. `corpus_docs` index: 1,788 embedded (59 new EO rows correctly skipped, empty text — see gap in WHAT IS COMPLETE). |
| Local generation tier | LIVE ✅ (flipped 09-06, this box only) | **This status is per-box, not global — see note below.** On primordial-galaxy: `.env`'s `LLM_MODE` flipped from a hard `venice` pin to `auto`; local now actually serves (`LOCAL_MODEL_FAST=gemma4:e2b`, `LOCAL_MODEL_PRECISE=gemma4:e4b`, swapped off `gemma4:12b` which benchmarked unusably slow on this CPU). The 65%-CPU/`num_gpu=31` throttle is removed here (`LOCAL_CPU_THREAD_CAP`/`LOCAL_NUM_GPU` default to `None` in `llm_client.py`, i.e. uncapped) — that cap existed specifically for the old box's RX580 TDR risk (`[[hardware-crash-under-local-compute]]`), which does not apply to this box's integrated-only graphics. **If the old Taichi X370/RX580 box is ever used again, the uncapped defaults here are wrong for it — re-set `LOCAL_CPU_THREAD_CAP`/`LOCAL_NUM_GPU` explicitly via env before running local generation there.** Full pipeline run confirmed working end-to-end on local generation (87 min, 63 items). |
| Playwright (scrapers) | LIVE ✅ (09-06, this box) | System deps needed manual `sudo` (venv's `playwright` isn't on root's `PATH` by default — use the full venv path: `sudo <repo>/.venv/bin/playwright install-deps chromium`). Once installed, CSDA scraper confirmed finding real items (previously hard-failed with missing-deps error); grants-browser portals navigate correctly. The old Windows Chromium binaries copied to `pg/models/ms-playwright` are dead weight on Linux — this box installed fresh via `playwright install chromium` into the default location instead. |
| Antibody pipeline (grounded) | LIVE ✅ (wired 09-06) | Was fully built but orphaned from `main.py` since inception — see WHAT IS COMPLETE. Now runs as Phase 3.5 on `high_value` opportunities, output to `data/procurement_shield_pending.json` (async review, not auto-promoted to the canonical `procurement_shield.json`). Verified against 1 real opportunity: VALIDATED, grounded, real FAR citation. Not yet tested at batch scale. |
| Git sync (primordial-galaxy) | ⚠️ NOT CONFIGURED | No SSH key, no credential helper on this box; HTTPS fetch fails (no auth). Jay's choice: PAT (over deploy key / `gh auth login`), not yet provided. All of v19's work is uncommitted, local-disk-only until this is set up. |
| Scheduling (primordial-galaxy) | ⚠️ NOT SET UP | No cron entry, no systemd timer. Windows `GovTechHunterDaily` has no equivalent here yet. v19 ran the pipeline manually every time. |
| FIPS 140 (primordial-galaxy) | LIVE ✅ | `fips_enabled=1`, running `5.15.0-190-fips`, Secure Boot on, TPM 2.0 present — verified 2026-09-05 (`pg/verify-2026-09-05*.txt`, PASS/GREEN both pre- and post-reboot). **Do not upgrade the kernel or FIPS packages without confirming FIPS validation holds** — see execution order item 0. |
| Cloud-failover scanner (mirror) | LIVE ✅ | Secrets set + workflow_dispatch tested 08-05; schedule green 08-05→08-10 (1 self-healed runner blip 08-06). |
| Models in stack | ⚠️ Chinese present | Daily-driver `qwen3-8b` + `qwen3.6-moe` = Chinese (Alibaba). `gemma`/`llama`/`anthropic`/`nomic-embed` = US. American-only scrub = priority #2. |
| M-26-04 compliance | STAMP FUNCTION ✅ | `compliance/disclosure_template.py:stamp_disclosure()` |
| SAM.gov API key | LIVE ✅ | Verified 2026-04-14. Rotation reminder ~2026-07-02. |
| Whitepaper | PUBLISHED ✅ | v2.1 live — apex-ronin/Rise-Of-The-Prompt-Kiddie (2026-06-04) |
| Cloud HUD (old) | SUSPENDED | The *cloud* dashboard — rebuild when cloud trigger fires. (Local Observatory above is the current observability surface.) |
| Brand | SETTLED ✅ | Sole operator. apexronin.com owned. apex-ronin GitHub org live. |
| LLC | DEFERRED | Post-revenue. Sole prop for now. |
| Scott Nelson | PERMANENTLY CLOSED | Own merit, own timeline. Do not contact. |

---

## WHAT NOT TO DO

- Do not contact Scott Nelson — permanently closed (2026-06-02). Own rodeo, own merit.
- Do not register domain before LLC name confirmed
- Do not reintroduce any `google.generativeai`, `google.cloud.aiplatform`, or `vertexai` calls — all LLM calls go through `execution/llm_client.py:complete()` (local → Venice → Anthropic cascade). Vertex/GCP are dead.
- Do not commit `peer_review_package/` — gitignored, contains legacy data files
- Do not open Antigravity — confirmed unstable, caused data loss
- Do not start Unclaimed Funds — parked 90+ days minimum
- Do not build a runtime TTSI compliance gate — TTSI_SPEC_v2 reviewed and killed 2026-06-03. M-26-04 compliance is human-review + stamp at current scale. Use `compliance/disclosure_template.py:stamp_disclosure()` on submission artifacts.
- Do not relitigate: aaas_poc.py = publication layer only, 13 vs 22 opportunities = not a bug. (NSF program-officer contact is now stripped from code → gitignored `.env`; do not re-hardcode.)
- **primordial-galaxy node (09-06):** do not upgrade the kernel or FIPS-related packages without confirming FIPS 140 validation holds first — this is a govtech-compliance box, "newest patch" is not automatically the safe default here.
- **primordial-galaxy node (09-06):** the uncapped `LOCAL_CPU_THREAD_CAP`/`LOCAL_NUM_GPU` (`None` = no cap) is correct for THIS box's integrated-only graphics, not a general policy change — if local generation ever runs again on the old RX580 box, re-apply an explicit cap via env first.
- **Do not `git push origin main` (or the full private history to `origin` in any form) — Open Decision #7 resolved 2026-09-09 to Option A, and this is a permanent rule under that model, not a temporary freeze.** `main` contains private-commercial-layer work (full Heartland `entity_procurement.py`, `set_mirror_secrets.ps1`) that must never reach `origin`. Only the separately curated `public` branch goes to `origin`, via reviewed cherry-pick. `git push jsnnlsn main` is fine and expected — `jsnnlsn` is the private mirror and is meant to carry the full history.

---

## SESSION LOG

| Session | Date | Key Actions | Commit |
|---|---|---|---|
| v1-v6 | 2026-02-05 to 2026-03-03 | Initial build, Hetzner deployment, Vertex migration v1 | various |
| v7 | 2026-03-23 | GTG-1002 realignment, Oracle hunter deployed | 1584b28 |
| v8 | 2026-03-31 | Grant Hunter rebuilt, R.O.N.I.N. integration mapped | 6c5b948 |
| v9 | 2026-04-01 | Planning meeting, B-1/B-2/B-3 fixed, antibody_agent built, corpus live | 2a970a2 |
| v10 | 2026-04-01 | Vertex migration complete, GTG-1002 preface, all decisions locked | 183aec2 |
| v11 | 2026-04-03-04 | Full audit, git cleanup, OMB doc recovered, roadmap system established | 7741d6b |
| handoff sprint | 2026-05-14 | Corpus 33→104, drafter→Claude Sonnet, corpus ingested, item6 complete, handoff package | 1d7816b |
| v12 | 2026-06-03 | TTSI_SPEC_v2 reviewed + killed; disclosure_template.py built; full roadmap audit + sync | — |
| v13 | 2026-06-10 | Handoff Items 3.1–3.4: AI-gate fix (>100-char silent keyword fallback root-caused — not an LLM outage), honest analysis_method tier label, diag scripts deleted, doc_fetcher.py document pull (login-wall aware). | — |
| v14 | 2026-06-11 | Sonnet legacy audit (43 findings) + fix pass: red_team_simulation.py & aaas_poc.py rewired Vertex/Gemini→cascade; disclosure_template.py model attribution fixed; README full rewrite; stale Vertex/Hetzner/Scott docs archived to `_ARCHIVE/`; dead Gemini/Vertex scripts → `_archive_vertex/`/`_archive_legacy/`; peer_review_package.zip untracked; FAR/oracle/whitepaper-preface fixes. | 46b72cf |
| v15 | 2026-06-12 | Audit round 2: 5 new findings (1 high/2 med/2 low) + 2 partials, fixed. Audit round 3: PASS-CLEAN, zero medium+ findings — 3-round loop converged. NSF program-officer PII final-stripped from all tracked files, `.env`-driven. Ready to flip working parts public on Jay's go. | 2b45872, 5b351c2 |
| v16 | 2026-06-16 | Built **Observatory** (SQLite spine + FastAPI live dashboard + non-fatal run recorder; verified auto-recording unattended). **govinfo.gov research + ingest** (74 corpus_docs seeded; EOs via Federal Register, GAO via govinfo). **Pre-public history audit** + reusable checklist — caught [REDACTED] PII in history (scrub staged Method A, not run). Decisions: keep git history (proof-of-work), invite collaboration openly, Conventional Commits. Surfaced: local tier down 3 days (logon-trigger) + Chinese daily-driver model. | — |
| v17 | 2026-08-09 (verified state as of 08-08/08-09, with Claude/chat — companion session, not this file's usual Code author) | **Confirmed public repo intact** (`apex-ronin/primordial-galaxy` live, 31 commits, nothing lost — false alarm about account being wiped, root cause was a bad web-search result compounded by exhaustion/no sleep). **Found + pulled** `scanner_daily.yml` (2 commits on `jsnnlsn/main`, private-mirror cloud-failover design, local rig crash under load was the driver) — local checkout was 2 behind, now fast-forwarded clean, zero conflicts. Confirmed remotes: `origin`=public, `jsnnlsn`=private mirror. Scan logs confirm pipeline ran unattended through 08-05 → 08-08 without intervention. **⚠️ CORRECTED IN v18: this session's "not yet done" list (secrets, workflow_dispatch) was wrong — both were already done 08-05, 4 days before this session, per STATE.md/HANDOFF_2026-08-05.md that this session didn't check.** | 82cad27 (pulled, not authored this session) |
| v19 | 2026-09-06 (Claude Code, new box: primordial-galaxy) | **Bootstrapped GovTech Hunter on a brand-new Linux node** (Ubuntu 22.04 FIPS, i5-10400, no discrete GPU — physically distinct from the old Taichi X370/RX580 rig; old box's `G:\AI-Models` tree pre-migrated to `pg/models` before this session). venv + deps installed, Ollama installed without root (reused pre-migrated model blobs, zero re-download), 3 hardcoded `G:\` paths made portable. **Found + fixed a real pre-existing bug**: semantic FAISS retrieval had been silently degrading to keyword-only fallback since the 08-10/08-24 Ollama rewire (stale embed URL convention + stale embedder name in 2 of 3 FAISS manifests) — fixed and verified live. **Right-sized the local model for this CPU**: `gemma4:12b` unusably slow here, swapped to `e2b`(fast)/`e4b`(precise); removed the old box's 65%-CPU/GPU throttle since its RX580-TDR failure mode doesn't exist on this hardware. Flipped `.env`'s `LLM_MODE` from a hard `venice` pin back to `auto` — local-primary cascade confirmed live. Got Playwright working on Linux (sudo PATH gotcha with venv-installed playwright). Refreshed EO corpus ingest (3 months stale; surfaced a separate pre-existing gap — new EOs have no full text, Federal Register's `abstract` field is empty, not something this session broke). **Biggest finding**: the flagship grounded antibody pipeline (`antibody_agent.py`/`red_team_simulation.py`, FAISS retrieval + grounding gate + specificity rubric) was fully built but **completely orphaned from `main.py`** — zero callers, `procurement_shield.json` untouched since 2026-06-11 despite 3 months of daily runs, because the old CLI blocked on `input()` (incompatible with unattended scheduling). Wired it in as a new Phase 3.5 (HIGH-fit only), replaced the blocking gate with an async pending-review file per Jay's choice, verified end-to-end against a real opportunity (VALIDATED, grounded, real FAR citation). Ran the full pipeline live end-to-end (63 opportunities, 87 min, all local generation). **Not done**: git sync (no credentials configured on this box, Jay deferred providing a PAT), daily scheduling (no cron/systemd timer set up yet), batch-scale test of the antibody wiring. **Flagged, not actioned**: a FIPS kernel update Jay saw available in a separate terminal — apt shows nothing pending as of this session, but treat any future kernel bump as a stop-and-ask item on this box, not routine. All of this session's changes are **uncommitted** (working tree only) pending git credential setup. | (uncommitted) |
| v19 (cont., 09-07) | 2026-09-06→07 (Claude Code, same session as v19, spans the date rollover) | **Antibody engine hardened for real**: added a named-enforcement-actor requirement + vector-specific drafting guidance to `antibody_agent.py`'s prompt (fixed a real SPEC GAP); rebuilt `economic_calibration` on real dollar units after finding it was structurally impossible to pass (comparing a 0-100 rubric score against a ~50x-by-construction ROI ratio); removed a wasted, discarded duplicate LLM call in `red_team_simulation.py`. **Corpus made real, not just present**: fixed EO full-text ingest (Federal Register's `abstract` field is empty for most EOs; wired `raw_text_url` instead), caught and fixed a NUL-byte SQLite silent-truncation bug in the process, and fixed an `upsert_corpus_doc` clobber bug that would've let a future scheduled re-ingest wipe already-filled text/embeddings. Added `observatory.ingest.fetch_ecfr_title48()` — FAR/DFARS now pull from eCFR's continuously-current bulk XML instead of an annual snapshot up to a year stale; ingested DFARS raw text for the first time ever (405 sections, code supported it, nobody had run it). **Found and fixed a real bug in this session's own work**: `embed_corpus.py` never called `load_dotenv()`, so every embed this session (including an earlier "successful" one) silently wrote into a throwaway directory instead of the real configured index — today's corpus improvements were never actually retrievable until this was caught; same latent pattern fixed defensively in `archive.py`/`entity_procurement.py` before it could bite later. **Live pipeline runs, for real**: found via a real 65-item run that 11% of scoring calls were silently degrading to the keyword-fallback placeholder (Jay's fail-flag #3) from a systematic JSON-parse failure class, including the one HIGH-fit signal that drove that run's entire antibody pipeline (confirmed a false positive on re-score); fixed with Ollama/Venice grammar-constrained JSON mode (`format: "json"`), re-verified 7/7 clean, then a full 66-item run cleared all three of Jay's fail-flags simultaneously for the first time. **Scheduling actually works, not just installed**: `run_scanner.sh` (daily 07:00) confirmed via a real unattended cron fire, not just a manual test; `run_corpus_refresh.sh` (weekly Sunday 06:00, eCFR+EO+GAO ingest→embed) built and verified end-to-end. **Permanent opportunity archive + delta pipeline** (Jay's directive): new `opportunity_documents` table + `observatory/archive.py`, every fetched RFP/grant archived to disk regardless of score with content-hash change detection; `main.py` Phase 2 now reuses already-scored links instead of re-fetching/re-scoring — live-verified (2 new, 50 reused on one real run). **Caught a second-order gap from that same fix**: Phase 3.5 (red-team+antibody) didn't check delta status, so a reused HIGH-fit opportunity got fully re-drafted on two consecutive runs, once producing a *worse* result than the original (LLM non-determinism) — fixed to only run on genuinely new HIGH-fit items; removed both duplicate pending-queue entries. **Governance**: audited and corrected two roadmap Open Decisions that were stale (#4 legal-corpus scope, #5 license — both already resolved back in 08-05 via the `ops` branch, never written back here); found and froze a live gap where `main` now mixes public-safe and private-commercial-layer commits with nothing enforcing the split (`git push origin` frozen pending a decision — see Open Decision #7); cleared `procurement_shield_pending.json`/`threat_assessment.json` of bad-data artifacts per Jay's explicit "start fresh" directive; verified (via a parallel session's portfolio audit, cross-checked live rather than trusted blind) that two public-repo exposure issues are now fixed, with the git-history purge and three other items still open in that other thread. Also: govinfo.gov developer-hub review (39 real collections vs. the curated subset the marketing page shows; flagged USCOURTS as the strongest future source for a 20-year procurement-fraud corpus, LSA for automatic regulation-change detection); entity_procurement verification extended to all 78,291 Census-of-Governments entities (was 58,742/32 states, now all 52 states+territories; 5,601 total verified with a real procurement page found); set up an SCP inbox folder (`/home/apexronin/inbox/`) for Taichi↔this-box file transfer. **Jay's explicit gate, recorded**: no outreach or AF Tech Connect submission until the pipeline runs consistently reliable — one clean run exists as a baseline, "consistently" does not yet. **Not done**: git commit/push (all of today's changes are still uncommitted pending a decision on the split model, same as v19's original close), the "daily agent training" 3-part plan beyond corpus refresh (nightly eval/regression suite + fine-tuning still just designed, not built), fraud-corpus sourcing (deliberately deferred, scoped as its own future repo). | (uncommitted) |
| v18 | 2026-08-10 (Claude Code) | **Corrected v17's stale premise** by cross-referencing `STATE.md` tail + `HANDOFF_2026-08-05.md`: cloud-failover secrets + workflow_dispatch were already done 08-05, re-verified green through 08-10 (`gh secret list`, `gh run list`). **Committed** the RFO ingest module (`dab4249`) and the mirror-secrets sync script (`310bf27`), both previously uncommitted/untracked. **Attempted** the pending `far_rfo` FAISS embed step — blocked: LM Studio daemon unreachable from this session (repeated timeout), independently confirmed down for the real 07:00 scan too (100% Venice-served, `logs/scan_2026-08-10_0700.log`). **Surfaced 5 items STATE.md had that this file never carried:** the FAR 52.204-21 dual-numbering regulatory correction, `LMStudioServerAtBoot` still unregistered, July SAM backfill, Arc #6 Heartland Phase 1 spec, and unconfirmed AF Tech Connect submission status. 4 scripts (`show_env_names.ps1`, `uncomment_venice.ps1`, `verify_rfo_ingest.py`, `probe_demolition.py`) still untracked, left for Jay's keep/delete call. **v18 continued, same session:** reviewed the 2026-07-29 "Ollama persistent process issue" thread (found via session search) and merged its pending items — confirmed it's the origin of scanner_daily.yml, closed the loop on its one unanswered question (keep both local-primary + cloud-failover, which is what actually happened). Jay then deleted LM Studio from the box (deliberate cleanup, confirmed not a full cloud pivot). **Unblocked the far_rfo embed for real:** repointed `embed_corpus.py` at Ollama's `nomic-embed-text`, CPU-pinned (`num_gpu:0`) after finding `OLLAMA_VULKAN=1` set on this box (same RX580-display-GPU TDR risk as LM Studio) — full 1,788-doc index rebuilt, all 1,060 far_rfo embedded. Swept Ollama's own model library for the American-models directive (found + deleted `qwen3.6` + `minimax-m3:cloud`, both Chinese) and deleted the entire June `_quarantined_models` folder (83GB) per Jay's updated delete-on-sight directive. Corrected `CLAUDE.md`'s Model Preferences section to match (also fixed a stale `qwen3-8b` reference that should've said `gemma-4-e4b` since 06-20). Surfaced but not actioned: local *generation* tier (`llm_client.py`) still points at LM Studio's dead port 1234 — Venice covers it fine, fast-follow not urgent. Voyage AI embedding-quality comparison discussed and agreed in principle but not started — no `VOYAGE_API_KEY` exists yet, needs Jay to get one. | dab4249, 310bf27, 4e8101b, 78d5616 |
